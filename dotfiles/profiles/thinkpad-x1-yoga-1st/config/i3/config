# i3 portable para ThinkPad X1 Yoga.
set $mod Mod4
set $term alacritty
set $menu rofi -show drun -show-icons
set $laptop_menu 9menu -popup -label "ThinkPad" -file ~/.config/9menu/laptop.menu

# BEGIN rafex theme
set $theme_bar_bg #e5e9f0
set $theme_bar_fg #2e3440
set $theme_separator #7b88a1
set $theme_active_bg #5e81ac
set $theme_active_fg #eceff4
set $theme_inactive_bg #d8dee9
set $theme_inactive_fg #4c566a
set $theme_urgent_bg #bf616a
set $theme_urgent_fg #eceff4
# END rafex theme

# BEGIN rafex i3-gaps
gaps inner 8px
gaps outer 10px
smart_gaps on
# END rafex i3-gaps

exec_always --no-startup-id xrdb -merge ~/.Xresources
# pasystray se inicia una sola vez mediante dex y /etc/xdg/autostart/pasystray.desktop
exec_always --no-startup-id dunst --config ~/.config/rafex/themes/current/dunst.conf
exec_always --no-startup-id setxkbmap es
exec --no-startup-id sh -c 'if [ -f "$HOME/Imágenes/FondosDePantalla/wallpaper.jpg" ]; then feh --bg-scale "$HOME/Imágenes/FondosDePantalla/wallpaper.jpg"; fi'
exec --no-startup-id sh -c 'command -v upower >/dev/null 2>&1 && ~/.local/bin/power-notify.sh'
exec --no-startup-id dex --autostart --environment i3
exec --no-startup-id udiskie --tray
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork
exec --no-startup-id nm-applet

font pango:DejaVu Sans Mono 9
default_border pixel 2
default_floating_border pixel 2

set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id ~/.local/bin/volume-notify.sh up && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id ~/.local/bin/volume-notify.sh down && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id ~/.local/bin/volume-notify.sh mute && $refresh_i3status
bindsym XF86Display exec --no-startup-id ~/.local/bin/screen-projector.sh --apply --mode next
# XF86Tools y XF86WakeUp se añaden por install-i3-laptop-controls.
bindsym $mod+F9 exec --no-startup-id $laptop_menu
bindsym $mod+Shift+t exec --no-startup-id ~/.local/bin/theme-toggle.sh --toggle

floating_modifier $mod
tiling_drag modifier titlebar
bindsym $mod+Return exec $term
bindsym $mod+q kill
bindsym $mod+space exec --no-startup-id $menu
bindsym $mod+Tab exec --no-startup-id rofi -show window -show-icons
bindsym $mod+Shift+Tab focus next

bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right
bindsym $mod+b split h
bindsym $mod+v split v
bindsym $mod+f fullscreen toggle
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split
bindsym $mod+Shift+f floating toggle; move position center
bindsym $mod+d focus mode_toggle
bindsym $mod+Shift+l exec --no-startup-id i3lock -c 000000

for_window [class="Thunar"] floating enable, resize set 800 600
for_window [window_type="dialog"] floating enable
for_window [window_role="pop-up"] floating enable
for_window [window_role="task_dialog"] floating enable
for_window [window_type="utility"] floating enable
for_window [window_type="toolbar"] floating enable
for_window [window_type="dialog"] move position center
for_window [window_type="dialog"] resize set 900 600

bindsym $mod+a focus parent
bindsym $mod+Shift+minus move scratchpad
bindsym $mod+minus scratchpad show
bindsym XF86LaunchA move scratchpad
bindsym XF86LaunchB scratchpad show
bindsym $mod+p exec --no-startup-id maim "$HOME/Imágenes/CapturasDePantalla/screenshot-$(date +%F_%H-%M-%S).png" && notify-send "Screenshot" "Pantalla completa guardada"
bindsym $mod+Shift+p exec --no-startup-id sh -c 'pgrep picom && pkill picom && notify-send "Picom" "Desactivado" || (picom & notify-send "Picom" "Activado")'

set $ws1 "1:term"
set $ws2 "2:code"
set $ws3 "3:web"
set $ws4 "4:docs"
set $ws5 "5:misc"
set $ws6 "6:media"
set $ws7 "7:comms"
set $ws8 "8:ops"
set $ws9 "9:monitor"
set $ws10 "10:scratch"

bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# Colocación por clase de ventana. Las aplicaciones no listadas permanecen
# en el escritorio actual para no mover ventanas desconocidas por accidente.
# Comprueba una clase nueva con: xprop WM_CLASS
assign [class="(?i)^alacritty$"] $ws1
assign [class="(?i)^kitty$"] $ws1
assign [class="(?i)^xterm$"] $ws1
assign [class="(?i)^(urxvt|rxvt-unicode)$"] $ws1
assign [class="(?i)^firefox$"] $ws3
assign [class="(?i)^(google-chrome|chromium|chromium-browser)$"] $ws3
assign [class="(?i)^(codium|vscodium)$"] $ws2
assign [class="(?i)^eclipse$"] $ws2
assign [class="(?i)^(zathura|evince|libreoffice|xournalpp)$"] $ws4
assign [class="(?i)^thunar$"] $ws5
assign [class="(?i)^(pavucontrol|nm-connection-editor|blueman-manager|arandr)$"] $ws5
assign [class="(?i)^(obs|guvcview|krita|mpv)$"] $ws6
assign [class="(?i)^(virt-manager|virtualbox)$"] $ws8

bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart
bindsym $mod+Shift+q exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"
bindsym $mod+Shift+e exec rofi -show run
bindsym XF86MonBrightnessUp exec --no-startup-id ~/.local/bin/brightness-notify.sh up
bindsym XF86MonBrightnessDown exec --no-startup-id ~/.local/bin/brightness-notify.sh down
bindsym XF86KbdBrightnessUp exec --no-startup-id ~/.local/bin/kbd-brightness-notify.sh up
bindsym XF86KbdBrightnessDown exec --no-startup-id ~/.local/bin/kbd-brightness-notify.sh down
bindsym $mod+Shift+s exec --no-startup-id ~/.local/bin/screensaver-toggle toggle

mode "resize" {
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym ntilde resize grow width 10 px or 10 ppt
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}
bindsym $mod+r mode "resize"

bar {
        status_command i3status
        position top
        tray_output primary
        font pango:DejaVu Sans Mono 10, Font Awesome 7 Free 10
        colors {
                background $theme_bar_bg
                statusline $theme_bar_fg
                separator $theme_separator
                focused_workspace $theme_active_bg $theme_active_bg $theme_active_fg
                active_workspace $theme_inactive_bg $theme_inactive_bg $theme_bar_fg
                inactive_workspace $theme_inactive_bg $theme_inactive_bg $theme_inactive_fg
                urgent_workspace $theme_urgent_bg $theme_urgent_bg $theme_urgent_fg
                binding_mode $theme_active_bg $theme_active_bg $theme_active_fg
        }
}
